[project]
name = "llm-from-scratch"
version = "0.1.0"
description = "A tiny character-level transformer, built up chapter by chapter."
requires-python = ">=3.11,<3.13"
dependencies = [
    "torch>=2.2",
]

[tool.uv]
package = false
